[project]
name = "lumi-agent"
version = "0.1.0"
description = "LUMI Guardian AI - Agent de monitoreo y seguridad para VPS"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
