[project]
name = "lumi-agent"
version = "0.1.0"
description = "Agente de telemetría para LUMI Guardián AI"
requires-python = ">=3.11"
dependencies = [
    "psutil>=5.9",
    "requests>=2.31",
    "python-socketio[client]>=5.11",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]