import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'app-vps-detail',
  template: `<p style="color: var(--color-text-secondary);">Configuración del VPS — se construye en el siguiente commit.</p>`,
})
export class VpsDetailComponent {}
